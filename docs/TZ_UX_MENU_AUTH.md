# ТЕХНИЧЕСКОЕ ЗАДАНИЕ (дополнение)

**Проект:** Telegram-бот для анализа договоров (p_d_a_b)  
**Задача:** Внедрение постоянных кнопок + навигации FSM + шаблонные сообщения + настройки + авторизация  
**Ограничение:** Новый функционал анализа/парсинга/истории НЕ добавлять. Меняем только UX, меню, тексты, переходы, настройки и авторизацию.

---

## 0. ЦЕЛЬ

Сделать бота «интерфейсом», а не чатом:

- постоянное меню (Reply Keyboard)
- продуманная навигация без тупиков
- понятные сообщения на каждом шаге
- аккуратное поведение при PROCESSING
- отдельный блок: Авторизация (доступ только сотрудникам)
- настройки бота: тексты, контакты support, лимиты, режимы

---

## 1. ПОСТОЯННОЕ МЕНЮ (Reply Keyboard, всегда)

Меню всегда видно, работает из любого состояния FSM:

```
[ 📄 Загрузка договоров ]   [ 📂 История анализов ]
[ 📊 Сравнение договоров ]  [ ℹ️ Информация ]
[ 💬 Поддержка ]            [ 🏠 Главная ]
```

**Правила:**

- меню показывать при /start и после любого экрана (attach keyboard всегда)
- при смене экранов использовать editMessageText (не плодить сообщения)
- нажатие кнопки меню должно корректно переводить пользователя на нужный экран, не ломая FSM

---

## 2. АВТОРИЗАЦИЯ (ОБЯЗАТЕЛЬНО, отдельный пункт)

**Цель:** бот доступен только сотрудникам Заказчика.

### 2.1 Механика авторизации (минимальная, без новых «продуктовых» функций)

- По умолчанию пользователь НЕ авторизован
- При /start (и при любом нажатии меню) проверять isAuthorized
- Если не авторизован: показать экран «Доступ ограничен» и остановить сценарий

### 2.2 Способы авторизации (реализовать один основной + резервный)

**A) Одноразовый корпоративный код (OTP)** (основной)

- админ в веб-админке генерирует одноразовый код (или задаёт статичный код в настройках, если генерация уже есть)
- пользователь вводит код в боте
- при успехе: сохраняем telegram_id в whitelist/authorized_users, выдаём роль user

**B) Белый список** (резервный/админский)

- admin может добавить telegram_id / username в whitelist через админку (уже есть Users/Access Requests)

### 2.3 Сообщения авторизации

- если не авторизован: бот показывает только экран авторизации + кнопку «Запросить доступ» (если есть), иначе просит код
- после успешной авторизации: показать главный экран + меню

### 2.4 Важные правила

- авторизация проверяется в каждом handler, который ведёт к UPLOAD/HISTORY/RESULT
- не авторизованному запрещено: загрузка, история, результат
- «Информация» и «Поддержка» можно показывать даже неавторизованным (по настройке `allow_public_info = true/false`)

---

## 3. УПРАВЛЯЮЩИЕ КНОПКИ FSM (универсальные)

Кнопки (Inline или Reply внутри сценария — на усмотрение, но меню Reply остаётся всегда):

- ◀️ Назад
- ❌ Отмена
- 🏠 Главная

**Поведение:**

| Кнопка | Действие |
|--------|----------|
| ◀️ Назад | возвращает на предыдущий «экран» текущего сценария; если предыдущего шага нет — ведёт на Главный экран (IDLE) |
| ❌ Отмена | полный сброс FSM, переход в IDLE, показать главный экран |
| 🏠 Главная | то же что Отмена, но без уточняющих текстов; всегда доступна |

---

## 4. ЭКРАНЫ + СООБЩЕНИЯ

Правило: каждый экран = одно сообщение (`editMessageText`), меню Reply прикреплено всегда.

### 4.1 Главный экран (IDLE)

**Триггеры:** /start (авторизован), 🏠 Главная, ❌ Отмена (после сброса)

**Текст:**
```
Главное меню.
Выберите действие:
• Загрузка — отправьте договор для анализа
• История — последние результаты
• Сравнение — скоро
• Поддержка — контакты
```

**Кнопки:** (кроме Reply меню) не обязательны.

---

### 4.2 Экран «Доступ ограничен» (UNAUTHORIZED)

**Триггеры:** /start (не авторизован), любое действие, требующее доступа

**Текст:**
```
Доступ к боту ограничен сотрудникам компании.
Введите одноразовый код доступа или обратитесь к администратору.

Если у вас нет кода — откройте «Поддержка».
```

**Ожидаемое действие:** пользователь отправляет текстом код или жмёт 💬 Поддержка

**Кнопки:** 💬 Поддержка, ℹ️ Информация (если `allow_public_info = true`)

---

### 4.3 📄 Загрузка договоров (UPLOAD_FILE)

**Триггеры:** 📄 Загрузка договоров (если авторизован, и не PROCESSING), переход из IDLE

**Текст:**
```
Загрузите договор или выбранные страницы договора.
Поддерживаются: PDF, DOC/DOCX, JPG/PNG (до 5 фото), ZIP.

Можно отправить один файл или архив. После загрузки начнётся анализ.
```

**Кнопки:** ❌ Отмена, 🏠 Главная

**Ветки:**

- A) Пользователь отправил файл корректного формата → переход в PROCESSING
- B) Неподдерживаемый формат/ошибка → экран «Ошибка файла» (4.6)
- C) 📂 История → переход в HISTORY (если не PROCESSING)
- D) 🏠 Главная/Отмена → IDLE

---

### 4.4 Обработка (PROCESSING)

**Триггер:** файл принят и началась обработка

**Текст:**
```
Документ получен.
Извлекаю текст и выполняю анализ…

Обычно это занимает до 1–2 минут (в зависимости от объёма).
```

**Кнопки:** ❌ Отмена НЕ делать (чтобы не ломать анализ, если текущая логика не поддерживает отмену). 🏠 Главная — не сбрасывает процесс.

**Правило PROCESSING при нажатии меню:** НЕ сбрасывать FSM, НЕ прерывать анализ, показывать BUSY-экран (4.5) или всплывающее сообщение.

---

### 4.5 Нажатие меню во время PROCESSING (BUSY-экран)

**Триггер:** любая кнопка меню во время PROCESSING

**Текст:**
```
Идёт обработка документа. Подождите, пожалуйста.
Результат появится автоматически после завершения.
```

**Действие:** остаёмся в PROCESSING, не меняем сценарий, меню Reply остаётся.

---

### 4.6 Ошибка файла (ERROR_FILE)

**Триггеры:** неподдерживаемый формат, повреждённый файл, превышен лимит фото > 5

**Текст:**
```
Файл не подходит.
Загрузите договор или выбранные страницы договора в формате PDF/DOC/DOCX/JPG/PNG или ZIP.
Лимит: до 5 изображений.
```

**Кнопки:** 📄 Загрузка договоров, 🏠 Главная

---

### 4.7 Результат анализа (RESULT)

**Триггер:** анализ завершён (существующая логика)

**Текст:**
```
Выжимка по договору:
(далее текущий вывод, без изменений логики)

Вы можете открыть «История анализов», чтобы вернуться к результату позже.
```

**Кнопки:** 📂 История анализов, 🏠 Главная

---

### 4.8 📂 История анализов (HISTORY)

**Триггер:** 📂 История анализов (если авторизован, и не PROCESSING)

**Текст (есть записи):**
```
История анализов (последние N):
1) {date} — {doc_name/short} — {status}
2) …
```

**Текст (пусто):**
```
История анализов пуста. Сначала выполните загрузку договора.
```

**Кнопки:** ◀️ Назад, 🏠 Главная

---

### 4.9 📊 Сравнение договоров (MVP-заглушка)

**Триггер:** 📊 Сравнение договоров

**Текст:**
```
Сравнение договоров будет доступно позже.
Сейчас доступна загрузка и история анализов.
```

**Кнопки:** 📄 Загрузка договоров, 📂 История анализов, 🏠 Главная

**FSM:** не запускаем новый сценарий, не создаём новые сущности.

---

### 4.10 ℹ️ Информация

**Триггер:** ℹ️ Информация

**Текст (шаблон):**
```
Бот помогает быстро получить структурированную выжимку по договору.
Поддерживаемые форматы: PDF, DOC/DOCX, JPG/PNG, ZIP.
Важно: бот не выполняет юридическую экспертизу.
```

**Кнопки:** 🏠 Главная

---

### 4.11 💬 Поддержка

**Триггер:** 💬 Поддержка

**Текст (шаблон, данные из настроек):**
```
Поддержка:
{support_name}
Telegram: {support_tg}
Email: {support_email}
Часы: {support_hours}

Опишите проблему и приложите файл/скрин, если нужно.
```

**Кнопки:** 🏠 Главная

---

## 5. НАСТРОЙКИ БОТА (в админке)

Добавить/уточнить настройки (раздел «Настройки бота» или «Настройки анализа»):

| Ключ | Описание |
|------|----------|
| welcome_text | текст главного экрана |
| info_text | текст «Информация» |
| support_name | имя поддержки |
| support_tg | Telegram поддержки |
| support_email | Email поддержки |
| support_hours | часы работы |
| allow_public_info | bool — можно ли показывать «Информация/Поддержка» неавторизованным |
| max_images | 5 (если не зашито) |
| processing_hint_text | текст ожидания |
| unauthorized_text | текст доступа |

**Правило:** настройки применяются сразу, без перезапуска.

---

## 6. ПРАВИЛА UX

- Один экран = одно сообщение
- Использовать editMessageText везде, где возможно
- Системные ответы «получен/анализирую» не дублировать (только обновлять текст)
- В PROCESSING не давать пользователю «убежать» в другие сценарии (только BUSY-экран)

---

## 7. ЗАДАЧИ ДЛЯ РЕАЛИЗАЦИИ (порядок)

### 7.1 Меню Reply Keyboard

- Добавить builder постоянного ReplyKeyboardMarkup с 6 кнопками
- Подключить меню при /start и прикреплять к каждому экрану
- Убедиться, что меню не пропадает при переходах FSM

### 7.2 Router/Handlers для кнопок меню

- Добавить единый обработчик текста кнопок меню (exact match)
- Проверка PROCESSING → BUSY-экран и return
- Проверка авторизации → UNAUTHORIZED-экран и return
- Корректный переход в нужное состояние

### 7.3 Авторизация

- Добавить middleware/guard `requireAuth(user_id)`
- Вызвать в UPLOAD_FILE, HISTORY, RESULT
- Реализовать экран UNAUTHORIZED
- Принимать код текстом (OTP) или whitelist-only

### 7.4 Управляющие кнопки (Назад/Отмена/Главная)

- ◀️ Назад: state.back() или карта переходов
- ❌ Отмена: state.clear(); показать IDLE
- 🏠 Главная: state.clear(); показать IDLE

### 7.5 Экранная система сообщений

- Вынести тексты экранов в константы (messages/config)
- Обновить тексты на шаблонные из ТЗ

### 7.6 PROCESSING: защита от «ухода»

- На нажатие меню показывать BUSY-экран
- Не сбрасывать FSM, не запускать другие состояния

### 7.7 Настройки бота (админка)

- Добавить поля support/info/welcome/unauthorized/max_images
- Подключить к экранам

### 7.8 Финальная проверка

- Неавторизованный: /start → UNAUTHORIZED → Support/Info (если allow_public_info) → загрузка запрещена
- Авторизованный: /start → IDLE → Загрузка → файл → PROCESSING → результат → История
- Во время PROCESSING: кнопка меню → BUSY → остаёмся в PROCESSING → результат

---

## 8. КРИТЕРИИ ПРИЁМКИ

- [ ] меню видно всегда
- [ ] все кнопки работают из любого состояния
- [ ] нет тупиков
- [ ] тексты понятные на каждом шаге
- [ ] PROCESSING защищён от сбросов
- [ ] авторизация блокирует доступ к загрузке/истории
- [ ] «Поддержка» доступна и настроена через админку

---

## 9. СВЯЗЬ С ТЕКУЩЕЙ ЛОГИКОЙ

Реализовать, не ломая суть существующей логики:

- **Бот:** `TelegramWebhookController` — обработка /start, /admin, document/photo. Авторизация через `BotUser::isApproved()`.
- **Модели:** BotUser (status pending/approved/rejected), ContractSetting (настройки), TelegramBot (welcome_message).
- **Админка:** Access Requests, Bot Settings, Contract Settings — расширить настройками support/info/unauthorized.
